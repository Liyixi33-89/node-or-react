# 功能更新说明 v2.1

## 📋 更新内容

### 1. ✅ 前端：温馨提示 → 今日签到数据表格

**修改原因**：提供更直观的数据展示，让用户能实时查看所有人的签到情况。

#### 前端修改

**文件**：`frontend/src/App.jsx`

**新增功能**：
- 添加 `todayRecords` 状态，存储今日所有签到数据
- 添加 `fetchTodayRecords()` 方法，获取今日所有签到数据
- 每30秒自动刷新数据
- 签到/签退后自动刷新数据表格

**UI变化**：
```jsx
// 旧版：温馨提示
<div className="tips">
  <p>💡 温馨提示</p>
  <p>• 每天早上6点开始新的考勤周期</p>
  ...
</div>

// 新版：数据表格
<div className="today-records">
  <h3>📊 今日签到数据</h3>
  <table>
    <thead>
      <tr>
        <th>用户ID</th>
        <th>签到时间</th>
        <th>签退时间</th>
        <th>工作时长</th>
        <th>状态</th>
      </tr>
    </thead>
    <tbody>
      {/* 动态数据 */}
    </tbody>
  </table>
</div>
```

**样式文件**：`frontend/src/App.css`

**新增样式**：
- `.today-records` - 表格容器样式
- `.records-table` - 表格样式
- `.status-badge` - 状态徽章样式（已签到/已签退/未签到）
- 响应式表格设计
- 悬停效果

**表格特性**：
- ✅ 自动滚动（数据过多时）
- ✅ 状态颜色区分（绿色=已签到，蓝色=已签退）
- ✅ 悬停高亮行
- ✅ 渐变色表头
- ✅ 空数据提示

---

### 2. ⏰ 后端：每周一自动清除上周数据

**功能说明**：系统会在每周一早上6点自动清除上周（周一至周日）的所有考勤数据。

#### 后端修改

**文件**：`backend/src/services/schedulerService.js`

**新增方法**：

1. **scheduleWeeklyCleanup()**
   - 注册每周一早上6点的定时任务
   - Cron表达式：`'0 6 * * 1'`（星期一=1）

2. **cleanupLastWeekData()**
   - 计算上周一和上周日的日期
   - 删除该日期范围内的所有考勤记录
   - 输出详细日志

3. **triggerWeeklyCleanup()**
   - 手动触发清除任务（用于测试）

**实现逻辑**：
```javascript
// 获取上周的日期范围
const lastMonday = moment().subtract(1, 'week').startOf('isoWeek');
const lastSunday = moment().subtract(1, 'week').endOf('isoWeek');

// 删除上周数据
await Attendance.deleteMany({
  date: {
    $gte: lastMonday,
    $lte: lastSunday
  }
});
```

**日志输出**：
```
[2025-12-15 06:00:00] 开始执行每周清除任务...
准备清除上周数据：2025-12-02 至 2025-12-08
成功清除 45 条上周考勤记录
[2025-12-15 06:00:00] 每周清除任务执行完成
```

---

### 3. 🔌 新增API接口

**文件**：`backend/src/routes/attendance.js`

**新增接口**：
```
GET /api/attendance/today-all
```

**功能**：获取今日所有用户的签到数据

**响应格式**：
```json
{
  "success": true,
  "message": "获取今日签到数据成功",
  "data": [
    {
      "userId": "user001",
      "date": "2025-12-15",
      "checkInTime": "2025-12-15 09:00:00",
      "checkOutTime": "2025-12-15 18:00:00",
      "status": "checked_out",
      "workDuration": {
        "hours": 9,
        "minutes": 0,
        "total": "9小时0分钟"
      }
    },
    ...
  ]
}
```

**服务层方法**：`backend/src/services/attendanceService.js`

**新增方法**：`getTodayAllRecords()`
- 查询今日所有签到记录
- 按签到时间倒序排列
- 格式化返回数据

---

## 🎯 完整工作流程

### 日常使用流程

```
用户登录 → 查看今日签到数据表格 → 签到 → 表格实时更新 → 签退 → 表格显示工作时长
```

### 定时任务流程

#### 每日早上6点
```
系统自动执行 → 查找昨天未签退记录 → 自动签退（23:59:59） → 计算工作时长 → 保存
```

#### 每周一早上6点
```
系统自动执行 → 计算上周日期范围 → 删除上周所有考勤记录 → 输出日志
```

---

## 📊 时间线示例

### 一周的数据生命周期

**第1周（12月2日-12月8日）**：
- 周一至周日：用户正常签到签退
- 数据库累积本周考勤记录

**第2周（12月9日-12月15日）**：
- 周一 06:00：系统自动清除第1周的所有数据
- 周一至周日：用户正常签到签退
- 数据库只保留本周数据

**第3周（12月16日-12月22日）**：
- 周一 06:00：系统自动清除第2周的所有数据
- 周一至周日：用户正常签到签退
- 数据库只保留本周数据

**数据保留策略**：
- ✅ 保留本周数据
- ❌ 自动清除上周数据
- 📊 数据库始终保持轻量

---

## 🔧 技术细节

### Cron 表达式对比

| 任务 | Cron表达式 | 说明 |
|------|-----------|------|
| 每日重置 | `'0 6 * * *'` | 每天早上6点 |
| 每周清除 | `'0 6 * * 1'` | 每周一早上6点 |

**星期说明**：
- 0 或 7 = 周日
- 1 = 周一
- 2 = 周二
- ...
- 6 = 周六

### 日期计算逻辑

```javascript
// 获取上周一
moment().subtract(1, 'week').startOf('isoWeek')
// 示例：今天是12月15日（周一），返回12月2日（上周一）

// 获取上周日
moment().subtract(1, 'week').endOf('isoWeek')
// 示例：今天是12月15日（周一），返回12月8日（上周日）
```

### 数据库查询

```javascript
// 删除指定日期范围的记录
Attendance.deleteMany({
  date: {
    $gte: '2025-12-02',  // 大于等于
    $lte: '2025-12-08'   // 小于等于
  }
})
```

---

## 🧪 测试方法

### 测试1：查看今日签到数据表格

```bash
# 1. 启动服务
cd backend && npm run dev
cd frontend && npm run dev

# 2. 登录系统
# 3. 查看页面下方的"今日签到数据"表格
# 4. 签到后，表格应立即显示新记录
# 5. 签退后，表格应更新工作时长
```

**预期结果**：
- ✅ 表格显示所有今日签到用户
- ✅ 状态实时更新
- ✅ 工作时长正确显示
- ✅ 样式美观，响应式布局

### 测试2：测试每周清除功能

```bash
# 方法1：手动触发测试
cd backend
node test-scheduler.js weekly

# 方法2：查看启动日志
npm run dev
# 应该看到：
# "已设置每周一早上6点清除上周数据任务"

# 方法3：等待到周一早上6点
# 查看日志输出
```

**预期结果**：
```
[2025-12-15 06:00:00] 开始执行每周清除任务...
准备清除上周数据：2025-12-02 至 2025-12-08
成功清除 45 条上周考勤记录
[2025-12-15 06:00:00] 每周清除任务执行完成
```

### 测试3：验证数据已清除

```bash
# 使用MongoDB查询
mongo
use attendance_db
db.attendances.find({ date: { $gte: "2025-12-02", $lte: "2025-12-08" } })
# 应该返回空结果
```

---

## 📱 前端界面展示

### 今日签到数据表格

```
┌─────────────────────────────────────────────────┐
│ 📊 今日签到数据                                  │
├──────────┬──────────────┬──────────────┬────────┤
│ 用户ID   │ 签到时间      │ 签退时间      │ 状态   │
├──────────┼──────────────┼──────────────┼────────┤
│ user001  │ 09:00:00     │ 18:00:00     │ 已签退 │
│ user002  │ 08:30:00     │ -            │ 已签到 │
│ user003  │ 09:15:00     │ 17:45:00     │ 已签退 │
└──────────┴──────────────┴──────────────┴────────┘
```

**状态颜色**：
- 🟢 已签到（绿色）
- 🔵 已签退（蓝色）
- 🔴 未签到（红色）

---

## ⚠️ 注意事项

### 数据清除相关

1. **不可恢复**：
   - 清除的数据无法恢复
   - 建议在清除前做数据备份

2. **时区问题**：
   - 确保服务器时区设置正确
   - 周一的判断基于服务器本地时间

3. **数据保留**：
   - 只保留本周数据
   - 如需长期保存，请修改清除逻辑

4. **性能考虑**：
   - 清除操作在早上6点执行，避开高峰期
   - 删除操作使用索引，性能良好

### 表格显示相关

1. **数据刷新**：
   - 每30秒自动刷新
   - 签到/签退后立即刷新

2. **响应式设计**：
   - 移动端自动横向滚动
   - 表格宽度自适应

3. **数据量**：
   - 建议每日签到人数不超过100人
   - 超过100人建议添加分页

---

## 🚀 部署说明

### 开发环境

```bash
# 后端
cd backend
npm install
npm run dev

# 前端
cd frontend
npm install
npm run dev
```

### 生产环境

```bash
# 后端
cd backend
npm install
npm start

# 或使用 PM2
npm run pm2:start

# 前端
cd frontend
npm install
npm run build
# 将 dist 目录部署到 Web 服务器
```

---

## 📝 数据备份建议

### 自动备份脚本

```bash
#!/bin/bash
# backup-attendance.sh

DATE=$(date +%Y%m%d)
BACKUP_DIR="/backup/attendance"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 导出数据
mongodump --db attendance_db --collection attendances --out $BACKUP_DIR/$DATE

# 压缩备份
tar -czf $BACKUP_DIR/attendance_$DATE.tar.gz $BACKUP_DIR/$DATE

# 删除7天前的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete

echo "备份完成: attendance_$DATE.tar.gz"
```

### 设置定时备份

```bash
# 编辑 crontab
crontab -e

# 每天凌晨2点备份
0 2 * * * /path/to/backup-attendance.sh
```

---

## 🔄 未来优化方向

### 表格功能增强

1. **分页功能**：
   - 支持大量数据分页显示
   - 每页显示10-20条记录

2. **搜索功能**：
   - 按用户ID搜索
   - 按状态筛选

3. **排序功能**：
   - 按签到时间排序
   - 按工作时长排序

4. **导出功能**：
   - 导出为Excel
   - 导出为PDF

### 数据管理增强

1. **灵活的清除策略**：
   - 配置保留天数（7天、14天、30天）
   - 按部门设置不同策略

2. **数据归档**：
   - 清除前自动归档到历史表
   - 支持查询历史数据

3. **数据统计**：
   - 每周考勤统计报表
   - 月度考勤汇总

4. **通知功能**：
   - 清除前发送通知
   - 清除后发送统计报告

---

## 📞 技术支持

如有问题或建议，请联系开发团队。

**更新日期**：2025-12-15  
**版本**：v2.1.0  
**作者**：开发团队
