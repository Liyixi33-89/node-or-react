# TaskManager 线上部署问题总结

> 部署时间：2026-03-04  
> 服务器：腾讯云 `服务ip`（OpenCloudOS）  
> 项目路径：`/var/www/taskmanager`

---

## 问题一：Nginx 配置文件路径错误

### 现象
前端访问页面异常，检查 Nginx 配置发现 `root` 路径指向错误目录。

### 原因
`nginx.conf` 中前端静态文件路径配置的是旧项目（考勤系统）的路径：
```nginx
root /var/www/attendance/frontend/dist;
```
实际路径应为：
```nginx
root /var/www/taskmanager/frontend/dist;
```

### 解决方案
修改 `nginx.conf`，将所有 `attendance` 相关路径替换为 `taskmanager`，包括：
- `root` 静态文件路径
- `access_log` 日志路径
- `error_log` 日志路径
- 文件注释说明

---


## 问题二：Nginx 配置文件不在预期位置

### 现象
```bash
cat /etc/nginx/conf.d/default.conf
# cat: /etc/nginx/conf.d/default.conf: No such file or directory
```

### 原因
服务器通过 `dnf install nginx`（系统包）安装，配置直接写在 `/etc/nginx/nginx.conf` 主配置文件中，**不会自动生成 `default.conf`**。

| 安装方式 | 配置文件路径 |
|---|---|
| 官方仓库安装 | `/etc/nginx/conf.d/default.conf` |
| dnf/yum 系统包安装 | `/etc/nginx/nginx.conf` |

### 解决方案
将本地修改好的 `nginx.conf` 上传到服务器：
```bash
# 方式一：scp 上传
scp nginx.conf root@服务ip:/etc/nginx/conf.d/taskmanager.conf
# 1. 创建配置文件
vim /etc/nginx/conf.d/taskmanager.conf 

# 方式二：直接写入
cat > /etc/nginx/conf.d/taskmanager.conf << 'EOF'
# 粘贴配置内容
EOF
```

---

## 问题三：Nginx 配置了 HTTPS/SSL 但无证书

### 现象
Nginx 配置文件中包含 SSL 相关配置，但服务器上没有 SSL 证书，导致 Nginx 无法启动。

### 解决方案
暂时注释掉 HTTPS + SSL 的 server 块，改用 HTTP（80 端口）配置：
```nginx
server {
    listen 80;
    server_name _;
    
    location / {
        root /var/www/taskmanager/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

> 后续可使用 `certbot` 申请免费 SSL 证书，再开启 HTTPS。

---

## 问题四：git pull 拉取代码卡住

### 现象
```bash
git pull origin test
# 卡住，Ctrl+C 中断
```

### 原因排查方向
1. 服务器无法访问 GitHub（国内服务器常见）
2. SSH 密钥未配置
3. HTTPS 协议需要 Token 认证
4. 代理/防火墙拦截

### 排查命令
```bash
# 查看远程地址协议
git remote -v

# 测试网络连通性
curl -I https://github.com --connect-timeout 10

# 查看详细日志
GIT_TRACE=1 git pull origin test
```

---

## 问题五：浏览器访问 IP 不成功（安全组未放行）

### 现象
- 本地 `ping 服务ip` 正常（网络通）
- 浏览器访问 `http://服务ip` 无响应

### 原因
服务器 Nginx 已正常监听 80 端口：
```bash
ss -tlnp | grep :80
# LISTEN 0  511  0.0.0.0:80  ...  nginx
```
但**腾讯云安全组未放行 80 端口**，外网请求被拦截。

### 解决方案
登录腾讯云控制台 → 安全组 → 入站规则 → 添加规则：

| 协议 | 端口 | 来源 | 策略 |
|---|---|---|---|
| TCP | 80 | 0.0.0.0/0 | 允许 |
| TCP | 443 | 0.0.0.0/0 | 允许（HTTPS 备用） |

---

## 问题六：注册接口返回 404

### 现象
前端调用 `POST /api/user/register` 返回 404。

### 原因
后端路由文件 `backend/src/routes/user.js` 中，注册路由被注释掉了：
```js
// 用户注册功能已暂时关闭
// router.post('/register', async (ctx) => {
//   ...
// });
```

### 解决方案
取消注释，开放注册路由：
```js
// 用户注册
router.post('/register', async (ctx) => {
  try {
    const { username, password, name } = ctx.request.body;
    if (!username || !password || !name) {
      ctx.body = { success: false, message: '请提供完整的注册信息' };
      return;
    }
    const result = await UserService.register(username, password, name);
    ctx.body = result;
  } catch (error) {
    console.error('注册接口错误:', error);
    ctx.status = 500;
    ctx.body = { success: false, message: '服务器错误' };
  }
});
```

重启后端服务：
```bash
pm2 restart all
```

---

## 部署检查清单

```bash
# 1. 检查 Nginx 配置语法
nginx -t

# 2. 重载 Nginx
systemctl reload nginx

# 3. 检查 80 端口监听
ss -tlnp | grep :80

# 4. 本地访问测试
curl -I http://localhost

# 5. 检查后端服务
pm2 status

# 6. 查看错误日志
tail -f /var/log/nginx/taskmanager_error.log
tail -f /var/log/nginx/taskmanager_access.log
```

---

## 经验总结

| # | 问题类型 | 关键点 |
|---|---|---|
| 1 | 配置路径错误 | 项目迁移时注意更新所有路径引用 |
| 2 | 配置文件位置 | 不同安装方式的 Nginx 配置路径不同 |
| 3 | SSL 证书缺失 | 没有证书先用 HTTP，后续再升级 HTTPS |
| 4 | git pull 卡住 | 国内服务器访问 GitHub 需要特殊处理 |
| 5 | 安全组未放行 | 云服务器必须在控制台开放对应端口 |
| 6 | 接口 404 | 后端路由被注释，需同步开放对应接口 |
